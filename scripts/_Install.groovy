println '''

  Thanks for installing the Pure CSS plugin!

  You can use the plugin in the following ways:
	* Run "grails copy-pure-css" to copy the CSS files over to your application
	* Use the Resources plugin to add the CSS to your GSPs ( <r:require module="pure-all" /> )

'''  
